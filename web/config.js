// Backend selection for Yalla Toktok.
// "firebase" = Firebase Authentication + Realtime Database (recommended here).
// "server"   = legacy Flask/SQLite backend using apiBase.
window.YALLA_CONFIG = {
  backend: "firebase",
  apiBase: ""
};
