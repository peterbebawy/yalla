# استضافة يلا توكتوك على Render

أرشّح هذا المسار لتجربة تشغيل محدودة للنسخة الحالية: خدمة Python مدفوعة بقرص دائم، والواجهة وAPI على نفس النطاق. هذا اختيار ملائم لبنية المشروع، وليس ادعاءً بأن Render أكثر أمانًا من كل المنافسين أو أن الاستضافة تعالج عيوب التطبيق.

## الإعداد

1. احتفظ بالكود في مستودع خاص على مزود Git تدعمه Render. وجود الكود على GitHub لا يعني أن قاعدة البيانات موجودة عليه؛ لا ترفع البيانات أو الأسرار للمستودع.
2. في Render اختر **New → Web Service** واربط المستودع.
3. اختر **Python 3**، وRoot Directory هو المجلد الذي يحتوي `server.py`؛ اتركه فارغًا لو رفعت محتويات المشروع لجذر المستودع.
4. Build Command:

```bash
pip install -r requirements.txt
```

5. Start Command:

```bash
gunicorn --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 30 server:app
```

6. اختر خطة مدفوعة تدعم Persistent Disk. أضف قرصًا عند `/var/data` وحدد حجمًا مناسبًا لبياناتك. لا تستخدم التخزين المؤقت مع SQLite؛ البيانات خارجه تضيع عند إعادة النشر أو إعادة التشغيل.
7. أضف متغيرات البيئة:

| المتغير | القيمة |
|---|---|
| `DATABASE_PATH` | `/var/data/toktok.sqlite` |
| `ALLOWED_ORIGINS` | `https://YOUR-SERVICE.onrender.com`؛ استبدله بعنوان خدمتك الحقيقي |
| `ENABLE_HSTS` | `1` بعد التأكد من عمل HTTPS |

أضف الدومين الخاص أيضًا إلى `ALLOWED_ORIGINS` عند استخدامه، وافصل بين النطاقات بفاصلة. اترك `web/config.js` بالقيمة `apiBase: ""` لتعمل الواجهة وAPI على نفس الموقع.

8. من **Shell** للخدمة، أنشئ حساب الإدارة:

```bash
python server.py --create-admin
```

9. افتح رابط الخدمة، وسجل دخول الأدمن من اختيار «عميل». جرّب حساب عميل ودريفر مستقلين واعتماد السائق ودورة رحلة كاملة.

## ما الذي توفره الاستضافة وما الذي يبقى على المشغّل؟

- Render توفر TLS/HTTPS وشهادات متجددة تلقائيًا. القرص الدائم مشفّر أثناء التخزين.
- التطبيق أضاف سياسة CSP تحد تحميل السكربتات إلى ملفات الموقع، ومنع التضمين داخل مواقع أخرى، وPermissions Policy تمنع الكاميرا والمايك وتسمح بالموقع لنفس الموقع. HSTS اختياري عند تفعيل المتغير المذكور.
- ما زالت الجلسات رموز Bearer في `sessionStorage`، وكلمات المرور مخزنة كمخرجات تجزئة. رؤوس الحماية طبقة إضافية وليست إثباتًا لعدم وجود ثغرات.
- فعّل التحقق بخطوتين لحساب الاستضافة وحساب مستودع الكود، وحدد أقل صلاحيات يحتاجها كل مسؤول. نفّذ نسخًا احتياطية متسقة لـSQLite بوسيلة النسخ الاحتياطي الخاصة بها، واحتفظ بنسخة منفصلة واختبر الاستعادة. لا تعتبر نسخ القرص بديلًا وحيدًا لنسخ قاعدة البيانات.
- حد المعدل الحالي للدخول والتسجيل يعتمد على `remote_addr`. خلف reverse proxy قد يُطبّق الحد على عنوان مشترك؛ يلزم ضبط عناوين الوكلاء الموثوقين/حدود الشبكة قبل فتح التسجيل للجمهور. لا تثق في `X-Forwarded-For` من أي مصدر عشوائي.
- يلزم قبل الإطلاق العام: إثبات ملكية الهاتف، سياسة كلمات مرور واستعادتها، إجراءات تحقق السائقين، سياسات خصوصية وتشغيل واحتفاظ بالبيانات، ومراجعة أمنية مستقلة واختبار حمل. النسخة لم تخضع لاختبار اختراق.

## حدود SQLite على القرص الدائم

هذا المسار لخدمة واحدة. القرص الدائم لا يدعم مشاركة البيانات بين عدة نسخ مستقلة، وقد يحدث توقف قصير عند إعادة النشر. عند التوسع، انقل طبقة البيانات إلى PostgreSQL مُدارة بعد تعديل الكود واختباره؛ لا يكفي تغيير اسم قاعدة البيانات في متغير البيئة.

## المراجع الرسمية

- [نشر Flask على Render](https://render.com/docs/deploy-flask)
- [شهادات TLS وإعادة التوجيه إلى HTTPS](https://render.com/docs/tls)
- [الأقراص الدائمة والتشفير وحدودها](https://render.com/docs/disks)

لم يتم إنشاء خدمة أو شراء خطة أو نشر المشروع على حسابك.
