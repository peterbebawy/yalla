# يلا توكتوك على Android

## حالة التسليم

تمت إضافة **مشروع Android بالمصدر** لنفس نسخة الموقع، ولم يتم إنتاج APK داخل هذه البيئة: Android SDK/Gradle/JDK compiler غير متاحة، وتعذر تنزيل Android SDK. لا يوجد ملف APK مزيف أو ملف ZIP باسم APK. يلزم رابط الموقع المستضاف HTTPS وإتمام البناء بالطريقة التالية.

التطبيق يعرض الموقع الحقيقي داخل WebView، بنفس اختيار عميل/دريفر والألوان والوظائف. لا يشغّل Python/SQLite داخل الموبايل. يحتاج إنترنت وسيرفر يعمل.

## بناء APK تجريبي من GitHub Actions

1. استضف النسخة المحدثة من المشروع حسب `DEPLOY_RENDER.md`، وسجّل رابطها HTTPS.
2. ارفع المشروع إلى مستودعك، بما فيه `android` و`.github/workflows/android.yml`.
3. افتح **Actions → Build Android trial APK → Run workflow**.
4. أدخل الرابط في `site_url` وشغّل البناء.
5. بعد نجاحه، نزّل `Yalla-Toktok-trial-APK` من **Artifacts**، فك الضغط، وستجد `app-debug.apk`.
6. ثبّته على جهاز Android 8 أو أحدث للتجربة. هذه نسخة تجربة باسم حزمة ينتهي بـ `.trial`، وليست نسخة نشر تجاري.

Workflow مرفق لكنه لم يُشغَّل من هنا ولم تُتحقق نتيجة بنائه. أول تشغيل فعلي مطلوب للتحقق من تجميع التطبيق. مفتاح debug على runner جديد قد يختلف في بناء لاحق؛ وقتها تحتاج إزالة النسخة التجريبية السابقة، وقد تفقد جلستها. قبل التوزيع استخدم مفتاح release ثابتًا كما يلي.

## APK موقّع للتوزيع

على جهاز به Android SDK Platform 35 وBuild Tools 35.0.0 وJDK 17 وGradle 8.11.1:

```bash
cd android
gradle -PyallaUrl=https://YOUR-SERVICE.onrender.com :app:assembleDebug
```

يمكن فتح مجلد `android` في Android Studio وتحديد Gradle 8.11.1 المحلي من إعداداته. لا تحتوي الحزمة على Gradle wrapper JAR؛ استخدم Gradle المثبّت أو ولّد wrapper من جهازك بالأمر `gradle -PyallaUrl=https://YOUR-SERVICE.onrender.com wrapper --gradle-version 8.11.1`.

لبناء release، استخدم معالج **Generate Signed App Bundle or APK** في Android Studio لإنشاء مفتاح توقيع خاص بك، أو اضبط متغيرات البيئة الأربعة التالية ثم نفّذ `assembleRelease`:

| المتغير | المطلوب |
|---|---|
| `YALLA_KEYSTORE` | المسار المطلق لملف مفتاح التوقيع |
| `YALLA_STORE_PASSWORD` | كلمة مرور الملف |
| `YALLA_KEY_ALIAS` | اسم المفتاح |
| `YALLA_KEY_PASSWORD` | كلمة مرور المفتاح |

```bash
gradle -PyallaUrl=https://YOUR-SERVICE.onrender.com :app:assembleRelease
```

المخرج: `android/app/build/outputs/apk/release/app-release.apk` عند ضبط التوقيع؛ بدون التوقيع ينتج ملف unsigned غير جاهز للتثبيت. احتفظ بالمفتاح وكلمات مروره خارج المستودع. لا تولّد مفتاحًا جديدًا لكل تحديث. زِد `versionCode` في `android/app/build.gradle` عند إصدار APK جديد.

## كيف تعمل التحديثات التلقائية؟

- تعدّل الموقع وتنشره على **نفس الرابط**. مجرد تعديل الملفات محليًا أو رفعها إلى مستودع غير مربوط بالنشر لا يحدّث الموبايل.
- التطبيق يحمل أحدث صفحات الموقع عند فتحه، ويمنع استخدام نسخة مخبأة من WebView.
- `web/updates.js` يفحص بصمة ملفات الواجهة من `/api/version` كل 60 ثانية أثناء ظهور الصفحة. عند تغيرها يعيد التحميل تلقائيًا إذا لم تكن هناك رحلة نشطة، نافذة حوار، أو بيانات غير مرسلة.
- أثناء رحلة أو كتابة بيانات يؤجل تحديث الواجهة ويعرض تنبيهًا. تغييرات الخادم قد تصبح نافذة فور نشرها؛ هذا التأجيل لا يعزل الرحلات عن تغييرات API أو قاعدة البيانات، لذلك يجب الحفاظ على توافق API عند النشر.
- أعد تشغيل workers بعد نشر الملفات، لأن بصمة الإصدار تحسب عند بدء السيرفر. استضافة Render تفعل ذلك ضمن إعادة النشر.
- لا يوجد تنزيل أو تثبيت APK تلقائي صامت. تغييرات الواجهة ومنطق السيرفر لا تحتاج APK جديدًا؛ أما الصلاحيات أو أيقونة التطبيق أو الكود الأصلي أو رابط الخدمة المضمّن فتحتاج APK جديدًا موقّعًا بنفس المفتاح.

## الصلاحيات والسلوك

- إنترنت وموقع أثناء استخدام التطبيق فقط. يطلب إذن الموقع عند استخدام ميزة تحديد المكان.
- لا يوجد تتبع موقع مضمون في الخلفية، ولا Push Notifications، ولا دفع أصلي Android.
- HTTPS فقط؛ أخطاء شهادات SSL تُرفض. روابط الخرائط الخارجية تفتح في تطبيق خارجي، والاتصال يفتح لوحة الاتصال للمستخدم.
- لا توجد واجهة `addJavascriptInterface` ذات صلاحيات Android؛ لا يُسمح بالتنقل داخل WebView إلى أصل مختلف عن موقعك.
- النسخ الاحتياطي التلقائي لبيانات التطبيق معطّل. جلسة الدخول الحالية تعتمد على `sessionStorage` في الويب، وقد يحتاج المستخدم تسجيل الدخول عند إعادة إنشاء التطبيق.
- لم يُختبر على جهاز Android أو محاكي. اختبر GPS، رفض الإذن، لوحة المفاتيح، الرجوع، المكالمات، الرسائل، تغيير الإصدار، وانقطاع الإنترنت قبل التوزيع.

## المراجع

- [Android: عرض تطبيق ويب داخل WebView](https://developer.android.com/develop/ui/views/layout/webapps/webview)
- [توافق Android Gradle Plugin 8.9 مع Gradle وJDK وSDK](https://developer.android.com/build/releases/agp-8-9-0-release-notes)
- [توقيع تطبيق Android](https://developer.android.com/studio/publish/app-signing)
