services:
  app:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      DATABASE_PATH: /app/data/toktok.sqlite
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-}
    volumes:
      - toktok-data:/app/data
    restart: unless-stopped
volumes:
  toktok-data:
